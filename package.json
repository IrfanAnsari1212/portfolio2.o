{
  "name": "irfan-ansari-portfolio",
  "version": "1.0.0",
  "description": "Portfolio of Irfan Ansari, Frontend Developer",
  "scripts": {
    "build:css": "tailwindcss -i src/input.css -o assets/styles.css --minify",
    "build:html": "node scripts/build-content.js",
    "build:admin": "cd admin-src && npm install --no-audit --no-fund && npm run build",
    "build": "npm run build:html && npm run build:css && npm run build:admin",
    "dev": "tailwindcss -i src/input.css -o assets/styles.css --watch"
  },
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "tailwindcss": "^3.4.19"
  },
  "private": true
}
